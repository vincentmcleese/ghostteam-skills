{
  "name": "agent-backup-github",
  "version": "1.0.0",
  "category": "ops",
  "description": "Daily automated backup of OpenClaw agent workspace/config to a private GitHub repository with secret-safe exclusions and restore workflow.",
  "tags": ["backup", "github", "disaster-recovery", "openclaw", "cron"],
  "requires": {
    "secrets": ["github_pat"],
    "tools": ["exec", "cron"],
    "bins": ["git", "rsync", "tar"]
  },
  "compatibility": {
    "openclaw": ">=2026.2.0"
  }
}
